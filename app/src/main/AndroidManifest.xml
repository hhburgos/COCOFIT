<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.cocofit">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/logo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/logo"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar">
        <activity android:name=".secciones.Memoria2">

        </activity>
        <activity android:name=".noticias.MenuNoticias" />
        <activity android:name=".DetailActivity" />
        <activity android:name=".noticias.MainActivity2" />
        <activity android:name=".subsecciones.FisicaTitulares" />
        <activity android:name=".subsecciones.EligeContinentesCapitales" />
        <activity android:name=".secciones.Capitales" />
        <activity android:name=".secciones.Calculo" />
        <activity android:name=".secciones.Memoria" />
        <activity android:name=".Menu" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>